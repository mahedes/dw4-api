nelmio_api_doc:
    documentation:
        info:
            title: API Biblio
            description: Données de la biliothèque du quartier
            version: 1.0.1
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
